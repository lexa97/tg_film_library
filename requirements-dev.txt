# Development and testing dependencies
# Includes all production dependencies plus dev tools

-r requirements.txt

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
pytest-mock>=3.14.0,<4.0.0
aiosqlite>=0.20.0,<1.0.0  # For in-memory SQLite in tests

# Code quality
ruff>=0.9.0,<1.0.0
