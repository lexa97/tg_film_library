BOT_TOKEN=your_bot_token_from_botfather
TMDB_API_KEY=your_tmdb_api_key

# Для локального запуска (без Docker):
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/tg_film_library

# Для Docker Compose (опционально; по умолчанию используются значения ниже):
# POSTGRES_USER=tgfilm
# POSTGRES_PASSWORD=tgfilm_secret
# POSTGRES_DB=tg_film_library
